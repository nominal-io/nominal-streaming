name: wheels
on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch: {}

env:
  CARGO_TERM_COLOR: always
  MATURIN_VERSION: v1.9.3

defaults:
  run:
    shell: bash

jobs:
  publish-wheel:
    name: publish-wheel (${{ matrix.platform.label }}, abi3 ${{ matrix.abi.python }})
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      fail-fast: false
      matrix:
        abi:
          - { python: "3.10", feature: python310 }
          - { python: "3.11", feature: python311 }
        platform:
          # --- manylinux (auto) ---
          - label: linux-x86_64
            os: linux
            runner: depot-ubuntu-24.04-4
            target: x86_64
            manylinux: auto
            env: {}

          - label: linux-aarch64
            os: linux
            runner: depot-ubuntu-24.04-arm-4
            target: aarch64
            manylinux: auto
            env: {}

          - label: linux-armv7
            os: linux
            runner: depot-ubuntu-24.04-4
            target: armv7
            manylinux: auto
            env: {}

          # --- musllinux ---
          - label: musllinux-x86_64
            os: musllinux
            runner: depot-ubuntu-24.04-4
            target: x86_64
            manylinux: musllinux_1_2
            env:
              OPENSSL_DIR: /usr
          - label: musllinux-aarch64
            os: musllinux
            runner: depot-ubuntu-24.04-4
            target: aarch64
            manylinux: musllinux_1_2
            env:
              OPENSSL_DIR: /usr
          - label: musllinux-armv7
            os: musllinux
            runner: depot-ubuntu-24.04-4
            target: armv7
            manylinux: musllinux_1_2
            env:
              OPENSSL_DIR: /usr

          # --- Windows ---
          - label: windows-x64
            os: windows
            runner: depot-windows-2025-4
            target: x64
            manylinux: "" # ignored
            python_arch: x64
            env:
              CARGO_FEATURE_VENDORED: "1"
              OPENSSL_SYS_VENDORED: "1"

          # --- macOS ---
          - label: macos-aarch64
            os: macos
            runner: depot-macos-15
            target: aarch64
            manylinux: "" # ignored
            env:
              # Pin the wheel's minimum macOS explicitly so the tag does not
              # follow the runner image. Matches the published macosx_11_0 wheels.
              MACOSX_DEPLOYMENT_TARGET: "11.0"
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Python (Windows)
        if: ${{ matrix.platform.os == 'windows' }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.abi.python }}
          architecture: ${{ matrix.platform.python_arch }}

      - name: Install Python (non-windows)
        if: ${{ matrix.platform.os != 'windows' }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.abi.python }}

      - name: Install OpenSSL (macOS)
        if: ${{ matrix.platform.os == 'macos' }}
        run: |
          set -euo pipefail
          brew install openssl@3
          echo "OPENSSL_ROOT_DIR=$(brew --prefix openssl@3)" >> $GITHUB_ENV
          echo "OPENSSL_LIB_DIR=$(brew --prefix openssl@3)/lib" >> $GITHUB_ENV

      # Build wheels (Linux/musllinux) with pre-install script & manylinux param
      - name: Build wheels (Linux family)
        if: ${{ matrix.platform.os == 'linux' || matrix.platform.os == 'musllinux' }}
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist -m py-nominal-streaming/Cargo.toml --no-default-features --features ${{ matrix.abi.feature }} -i python${{ matrix.abi.python }}
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
          manylinux: ${{ matrix.platform.manylinux }}
          maturin-version: ${{ env.MATURIN_VERSION }}
          before-script-linux: |
            if command -v yum &> /dev/null; then
              yum update -y && yum install -y perl-core openssl openssl-devel pkgconfig libatomic
              if [[ ! -d "/usr/lib64" ]]; then
                ln -s /usr/lib/libatomic.so.1 /usr/lib/libatomic.so
              fi
            else
              apt update -y && apt-get install -y libssl-dev openssl pkg-config
            fi
        env: ${{ matrix.platform.env }}

      # The Linux legs build inside manylinux/musllinux containers that carry
      # their own Rust. Windows and macOS build natively against whatever the
      # runner image provides, and Depot's Windows image ships rustup with no
      # default toolchain, so maturin cannot pick a rustc. Select one
      # explicitly rather than depending on the image's preinstalls.
      - name: Install Rust toolchain (Windows/macOS)
        if: ${{ matrix.platform.os == 'windows' || matrix.platform.os == 'macos' }}
        uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1
        with:
          toolchain: stable
          # Installing the toolchain takes ~10s, while the cache save step
          # costs 19s on Windows and 36s on macOS and captured nothing across
          # a full dry run. Caching here is net slower, so leave it off.
          cache: false
          rustflags: ""

      # Build wheels (Windows/macOS)
      - name: Build wheels (Windows/macOS)
        if: ${{ matrix.platform.os == 'windows' || matrix.platform.os == 'macos' }}
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.platform.target }}
          args: --release --out dist -m py-nominal-streaming/Cargo.toml --no-default-features --features ${{ matrix.abi.feature }} -i python
          sccache: ${{ !startsWith(github.ref, 'refs/tags/') }}
          maturin-version: ${{ env.MATURIN_VERSION }}
        env: ${{ matrix.platform.env }}

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          version: "0.8.x"
          enable-cache: false

      # Only publish for release tags. This keeps workflow_dispatch usable as a
      # build-only dry run, which is the only way to exercise the wheel matrix
      # without pushing to PyPI.
      - name: Publish wheel
        if: ${{ startsWith(github.ref, 'refs/tags/') }}
        run: |
          uv publish --token ${{ secrets.PYPI_TOKEN }}
