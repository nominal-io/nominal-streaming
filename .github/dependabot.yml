version: 2
updates:
  - package-ecosystem: "cargo"
    directory: "/"
    schedule:
      interval: "daily"
    groups:
      external-deps:
        exclude-patterns: ["nominal-api"]
        applies-to: version-updates
        update-types:
          - "minor"
          - "patch"
