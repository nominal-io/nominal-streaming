#!/usr/bin/env bash
set -e
if [[ ! -d "/home/balattar/sources/nominal-streaming" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/home/balattar/sources/nominal-streaming")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/home/balattar/sources/nominal-streaming" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/home/balattar/sources/nominal-streaming/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/home/balattar/sources/nominal-streaming/.envrc" "/home/balattar/sources/nominal-streaming/.direnv"/*.rc
