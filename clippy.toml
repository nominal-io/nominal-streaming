# TODO(awatwe): port HTP's disallowed-methods rule (no unwrap/expect) after
# nominal-streaming source is cleaned up. There are ~100 call sites today; a
# follow-up PR will replace them with `?` and proper error handling, then add:
#
# disallowed-methods = [
#   { path = "core::result::Result::unwrap", reason = "handle errors explicitly via ? or match" },
#   { path = "core::option::Option::unwrap", reason = "handle errors explicitly via ? or match" },
#   { path = "core::result::Result::expect", reason = "handle errors explicitly via ? or match" },
#   { path = "core::option::Option::expect", reason = "handle errors explicitly via ? or match" },
# ]
